using System;

void main()
{
    byte[] arr = new byte[3];
    for (int i = 0; i < arr.Length; ++i)
    {
        arr[i] = cast<byte>(i);
    }
    foreach (byte b in arr)
    {
        byte x = b;
    }
}
