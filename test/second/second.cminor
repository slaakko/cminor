using System;
using System.IO;
using System.Unicode;

void main()
{
    Stream stream = new FileStream(stdout, FileAccess.write);
    Utf8Encoder encoder = new Utf8Encoder(stream);
    string s = "Hello, World!\n";
    encoder.Encode(s);
}
